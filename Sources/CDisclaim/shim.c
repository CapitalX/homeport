#include "CDisclaim.h"

/* No implementation needed: responsibility_spawnattrs_setdisclaim lives in
 * libSystem. This translation unit exists only so SwiftPM has a C source to
 * compile for the CDisclaim target and exposes the public header to Swift. */
